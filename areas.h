
/* Meza Martinez Emmanuel
   No. Cuenta: 311309649*/

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define PI 3.1416

#define cir(radio) (PI * radio * radio)
#define tri(base,altura) ((base * altura) / 2)
#define cua(lado) (lado * lado)
#define rec(base,altura) (base * altura)

float circulo(float);
float triangulo(float,float);
float cuadrado(float);
float rectangulo(float,float);



